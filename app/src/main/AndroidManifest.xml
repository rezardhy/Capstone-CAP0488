<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reza.capstonecap0488">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CapstoneCAP0488">
        <activity android:name=".presentation.ui.searchpage.image.ImageActivity"></activity>
        <activity android:name=".presentation.ui.searchpage.identification.IdentificationActivity" />
        <activity android:name=".presentation.ui.searchpage.suggestion.SuggestionActivity" />
        <activity android:name=".presentation.ui.homepage.article.ArticleActivity" />
        <activity android:name=".presentation.ui.searchpage.result.ResultActivity" />
        <activity
            android:name=".presentation.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>